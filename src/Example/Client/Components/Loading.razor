@if(IsLoading)
{
    <p>Loading...</p>
}
else
{
    @ChildContent
}

@code {
    [Parameter]
    public bool When { get; set; }

    private bool IsLoading => When;

    [Parameter]
    public RenderFragment ChildContent { get; set; }
}